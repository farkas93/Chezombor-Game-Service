apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "game-platform.fullname" . }}
  labels:
    {{- include "game-platform.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}